# Канон стенда — контракт с текстом курса

> Имена сущностей, статусы и числа сцен должны совпадать в стенде и в тексте
> шагов **буква в букву**: студент сверяет свой вывод с курсом. Этот файл —
> источник правды для обеих сторон. Меняется вместе со стендом, а не после.

## Домен

Служба доставки. Сущности и их имена в выводе стенда:

| Сущность | В timeline и логах | В базе |
|---|---|---|
| клиент | `client` | `orders.client` |
| заказ | `orders` (модуль монолита) | таблица `orders` |
| платёж | `payments` (модуль монолита) | таблица `payments` |
| эквайринг | `acquirer` (внешний) | журнал списаний сервиса |
| курьер | — | таблица `courier_assignments` |
| уведомление | — | таблица `notifications` |

**Статусы заказа:** `created` → `paid` → `assigned`, и `cancelled` — куда
приходит заказ, чей длинный процесс сломался и был компенсирован. `assigned`
и `cancelled` появились в стенде вместе с профилем `broker` (m06–m07); статуса
`delivered` в стенде нет, и в тексте его писать нельзя.

**Каталог ресторанов** — второй сценарий службы, дешёвое чтение:
`Пекарня «Батон»`, `Плов-хаус`, `Суши на углу`, `Кофейня «Пар»`,
`Бургерная «Гриль»`. Нужен там, где надо показать, что исправная часть системы
страдает от неисправной: общий отказ процесса (сцена 4), каскад (сцена 10),
перегрузка (сцены 2, 11), остановка (сцена 12).

**Сервис уведомлений** (`notifier`, профили `split` и `net`) — тот же модуль,
что жил внутри процесса, вынесенный за сеть. Своими данными владеет сам:
монолит в них не заглядывает и таблицы `notifications` в профиле `split` не
пишет. Идентификаторы уведомлений — `n_1`, `n_2`, …

**Внешний провайдер пуш-уведомлений** своего контейнера не имеет: он и в жизни
за периметром. Его поведение задаётся сервису уведомлений (`provider_mode`,
`provider_retries`), а наблюдаемая величина — число попыток к нему.

**Статусы платежа:** `pending` → `charged`. Отказ провайдера — `failed`
(в сценах пока не встречается).

**Курьеры:** `Айгуль`, `Пётр`, `Никита`. Назначение — по номеру заказа
(`номер % 3`), а не по счётчику: так оно остаётся детерминированным, даже
когда два заказа идут одновременно.

**Клиент и ресторан сцены 1:** `Ирина`, `Пекарня «Батон»`.

## Формат timeline

```
t=0.000  client    → orders     POST /orders {idempotency_key: none}
t=0.005  payments  ⋯ acquirer   POST /v1/charge             [задержка 30s]
t=5.000  client    ✗ orders     таймаут клиента (5s), исход НЕИЗВЕСТЕН
t=30.005 payments  ← acquirer   200 OK {charge: ch_1}       ← первая попытка ЖИВА
ИТОГ: заказов 2, списаний 2, клиент заплатил 3780 вместо 1890 …
```

- **Слева от стрелки — тот, кто наблюдал событие; справа — вторая сторона
  обмена.** Это единственное правило, из которого следуют все четыре стрелки.
- `→` вызов · `←` ответ · `⋯` незавершённое ожидание · `✗` отказ или таймаут.
- Время — от начала сцены, всегда три знака после запятой.
- Последняя строка — итог сцены обычными словами.
- Никаких ANSI-цветов: вывод копируется в текст шага как есть.

> В образце `LAB.md` ответ провайдера записан как `acquirer → payments`.
> Стенд печатает его как `payments ← acquirer`: нормативный список стрелок
> из той же спецификации различает вызов и ответ, и «наблюдатель слева»
> делает все четыре стрелки одним правилом вместо двух конкурирующих.

## Откуда берутся числа

**Времена — сценарные, содержание — наблюдаемое.** Прогон измеряет каждое
событие по-настоящему и сверяет со сценарием; печатает при этом сценарное
значение. Если наблюдение вышло за допуск (±1–2 с) или событие не случилось
вовсе, сцена печатает блок `⚠ СЦЕНА РАЗОШЛАСЬ СО СЦЕНАРИЕМ` и возвращает
ненулевой код — вывод не годится для сверки с текстом шага.

Так решается противоречие: система настоящая (сеть, база, конкурентность —
всё реально дрожит на единицы миллисекунд), а текст шага обязан совпадать с
терминалом студента посимвольно. Реальные измерения видны в
`./lab scene <id> --explain`, блок `ХРОНОМЕТРАЖ`.

**Идентификаторы и итоги — настоящие.** Номера заказов идут из
последовательности базы (`7714`, `7715`), идентификаторы списаний — из
журнала эквайринга (`ch_1`, `ch_2`), числа в строке `ИТОГ` считаются из базы
службы и журнала провайдера. UUID в стенде нет нигде: студент должен узнать
`order: 7714` из текста шага в своём выводе.

## Вторая форма вывода — таблица замеров

Там, где предмет сцены есть обмен, вывод читается лентой. Там, где предмет —
число под нагрузкой, лента бесполезна: нужен столбец, который растёт. Такие
сцены печатают таблицу (`"render": "table"` в сценарии).

Правило то же самое: **печатаются сценарные значения, наблюдение сверяется с
ними по допуску**. Допуск задаётся в сценарии на каждый замер — абсолютный
(`tol`), относительный (`tol_pct`) или граничный (`min`/`max`). Что именно
наблюдалось, показывает `./lab scene <id> --explain`, блок `СВЕРКА`.

## Как стенд создаёт нагрузку

Генератора-контейнера в стенде нет: поток подаёт `labctl`. Два свойства этого
потока — предмет курса, а не деталь реализации, и в тексте шагов они названы:

- **Модель открытая.** Заявки приходят по расписанию и не ждут, пока
  освободится предыдущая. В закрытой модели (N клиентов по кругу) очередь не
  растёт никогда, и закон Литтла показать нечем.
- **Промежутки экспоненциальные, зерно фиксированное** (`20260809`). При
  идеально ровном потоке и постоянном времени обработки очередь не возникает
  вовсе до самого потолка: очередь на 80% утилизации рождается из разброса.
  Фиксированное зерно делает последовательность одинаковой во всех прогонах.

**Время обработки задаётся сценой и отсчитывается от момента, когда обработчик
взялся за запрос.** Запись ответа делается уже освободившимся обработчиком —
иначе настоящая ёмкость службы оказывается ниже расчётной, и число «потолок
ровно N rps» в шапке сцены становится враньём. Накладные расходы всё равно
добавляют 3–4 мс на запрос: настоящий потолок на 3–4% ниже расчётного, и там,
где это важно (сцена 2), об этом сказано прямо в выводе.

## Обмен сообщениями (профили `broker` и `cdc`, m06–m07)

**Кто отправляет.** С m06 события публикует сервис заказов — `orders`. Он же
и владелец данных: своя база (`orders` в том же кластере PostgreSQL, отдельная
от `delivery`), своя таблица исходящих сообщений. Монолит в этих сценах не
участвует: к m06 заказы из него уже выделены, и вводить в кадр обе версии
службы сразу — значит объяснять студенту стенд вместо предмета.

**Кто читает.** Два подписчика на одном и том же событии, и именно из этого
обмен становится веерным:

| Сервис | Что делает | Появляется |
|---|---|---|
| `courier` | назначает курьера по номеру заказа | m06 |
| `notifier` | тот же сервис уведомлений, но с подпиской вместо вызова | m06 l02 |
| `outbox-relay` | читает исходящий ящик, публикует, отмечает | m07 l02 |

**Имена, которые видит студент.** Меняются только вместе с текстом шагов:

| Что | Имя | Где живёт |
|---|---|---|
| обменник | `orders` | модель очереди |
| очередь курьеров | `courier` | модель очереди |
| очередь уведомлений | `notifier` | модель очереди |
| обменник разбора | `orders.dead` | модель очереди |
| очередь разбора | `courier.dead` | модель очереди |
| очередь ручного разбора | `manual-review` | модель очереди |
| лог событий | `order-events`, 3 партиции | модель лога |
| группа курьеров | `courier` | модель лога |
| группа сверки | `audit` | модель лога |

**Типы событий:** `order.paid`, `order.assigned`, `order.cancelled`. Все три —
факты в прошедшем времени: отправитель не знает, кто на них подпишется, и
ничего не ждёт в ответ. Ключ партиционирования — номер заказа: всё, что
случилось с одним заказом, обязано лечь в одну партицию.

**Идентификатор события** — `evt_<номер заказа>` (например `evt_7714`). По нему
дедуплицирует идемпотентный потребитель. У повторной доставки он тот же самый,
и у повторной публикации из исходящего ящика — тоже: в этом весь смысл.

**Курьеры и назначение** — те же, что и в монолите: `Айгуль`, `Пётр`, `Никита`,
по правилу `номер заказа % 3`. Правило не меняется при выделении сервиса
намеренно: студент должен узнать в новом сервисе прежнее поведение.

**Кворумные очереди.** Очереди объявляются кворумными, потому что предел
повторных доставок (`x-delivery-limit`) есть только у них. Без него сцену
«отравленное сообщение ушло в очередь разбора» пришлось бы играть руками
потребителя — и она показывала бы не механику брокера, а самодельный счётчик.

**Каждая сцена пересоздаёт топологию и лог целиком.** Очереди удаляются и
объявляются заново, тема удаляется и создаётся заново с нужным числом
партиций. Сцена, которая начинается с чужих сообщений, не годится для шага:
студент сверяет число записей с текстом.

## Сцены 13–16 — числа для текста шагов (m06)

| Сцена | Что задано | Что наблюдается |
|---|---|---|
| 13 | 5 событий, заказы `7714`…`7718` (очередь) и `7719`…`7723` (лог) | в очереди после чтения 0, в логе 5; группа `audit` читает 5 |
| 14 | 3 события, заказы `7714`…`7716`; смерть после 2-го | обработок 4 на 3 сообщения, заказ `7715` назначен дважды |
| 15 | 3 события ×2 прогона; необрабатываемые — `7715` и `7718`; предел `3` | без предела 1 обработано, 2 нет; с пределом 2 обработано, 1 в разборе |
| 16 | 3 события заказа `7714`: `paid`(300 мс) → `assigned`(100 мс) → `cancelled`(10 мс) | 1 обработчик — итог `cancelled`; 4 обработчика — итог `paid` |

**Сцена 16 детерминирована**, хотя показывает гонку. Времена обработки заданы
самими событиями, поэтому перестановка воспроизводится каждый раз. В тексте шага
это надо сказать прямо: в жизни времена тоже разные, просто заранее неизвестные,
и тогда неверным оказывается не каждый прогон, а какой-нибудь.

**Курьер в сценах m06 держит статус заказа по типу события:** `order.paid` →
`paid`, `order.assigned` → `assigned`, `order.cancelled` → `cancelled`. Это его
собственная проекция, а не таблица владельца данных, — и в тексте её нельзя
называть «статусом заказа» без оговорки, чьим.

## Захват изменений из журнала базы (профиль `cdc`, m07 l03)

Рядом с базой встаёт коннектор, который читает её журнал репликации. Сервис
заказов при этом не меняется вовсе: в сцене 19 у него режим записи `none` —
ни прямой отправки, ни исходящего ящика.

**Имена, которые видит студент:**

| Что | Имя |
|---|---|
| тема потока изменений | `cdc.public.orders` — `<префикс>.<схема>.<таблица>` |
| слот репликации | `lab_orders` |
| публикация | `lab_orders_pub` |
| действующее лицо в timeline | `connector` |

Имя коннектора в каждом прогоне своё (смещения удалённого коннектора остаются
в служебной теме, и одноимённый продолжил бы с прошлого места вместо снимка) —
поэтому в выводе сцены и в тексте шага оно не называется.

**Что наблюдается в записях** (проверено на Debezium 3.6.1, `pgoutput`,
`REPLICA IDENTITY DEFAULT` — то есть на настройках по умолчанию):

- Тип операции — `op`: `r` (строка из начального снимка), `c`, `u`, `d`.
  Доменного типа события (`order.paid`) в потоке нет.
- У `op=u` поле `before` — **`null`**: прежнего значения строки журнал по
  умолчанию не хранит, и «что именно изменилось» из записи не следует.
- У `op=d` поле `after` — `null`, а `before` содержит **только первичный ключ**;
  остальные поля заполнены пустышками своего типа: `""`, `0`,
  `"1970-01-01T00:00:00.000000Z"`.
- Имена и типы полей приходят из схемы таблицы. `ALTER TABLE` с этого момента —
  изменение публичного контракта.

**Сцена 19 — числа для текста шага:**

| Что задано | Что наблюдается |
|---|---|
| 2 заказа (`7714`, `7715`) созданы до подключения коннектора | начальный снимок: 2 записи `op=r` |
| третий заказ (`7716`) создан при работающем коннекторе | 1 запись `op=c` |
| одна команда `UPDATE … WHERE restaurant='Пекарня «Батон»'` | 3 записи `op=u`, по одной на строку |
| `DELETE FROM orders WHERE id=7714` | 1 запись `op=d` |
| приложение не публикует ничего | в очереди `courier` 0, в логе `order-events` 0, в теме `cdc.public.orders` 7 |

Служебные поля (`lsn`, `txId`, метки времени) в выводе сцены сокращены до `…`:
они меняются каждый прогон. Об этом сказано в самом выводе, и в тексте шага
это надо повторить — иначе студент будет искать у себя те же числа.

## Сцена 1 — числа для текста шага (m01 l04)

| Что | Значение |
|---|---|
| Первый заказ | `7714`, второй (повтор) — `7715` |
| Сумма заказа | `1890` |
| Задержка эквайринга | `30s` (ровно, из конфигурации сцены) |
| Таймаут клиента | `5s`, один повтор |
| Списания | `ch_1` и `ch_2` |
| Курьеры | `7714` → Пётр, `7715` → Никита |
| Итог | заказов 2, списаний 2, заплачено 3780 вместо 1890 |

Эталонный вывод целиком — [`scenes/expected/scene-01.txt`](../scenes/expected/scene-01.txt).
Он и есть то, что копируется в шаг под спойлер: ни одно число в тексте
не пишется по памяти.

## Копии данных (профили `cluster` и `cache`, m08–m09)

С m08 у данных больше одной копии, и вместе с копиями появляются вопросы,
которых до сих пор не было: что видно на второй копии, сколько копий обязаны
подтвердить запись и кто главный, когда связи нет.

**Узлы, которые видит студент:**

| Имя | Кто это | Профиль |
|---|---|---|
| `kafka-1`, `kafka-2`, `kafka-3` | три узла брокера; роли брокера и голосующего совмещены | `cluster` |
| `postgres` | первичный узел базы | все |
| `postgres-replica` (в timeline — `replica`) | вторая копия базы | `cluster` |
| `redis` | быстрая память рядом с базой | `cache` |

**Брокер в кластере — тот же самый, что в m06.** Смена продукта посреди курса
объясняла бы студенту стенд вместо предмета. Куча по 320 МБ на узел, три узла
плюс база и сервис заказов укладываются в потолок профиля.

**Вторая копия базы отстаёт на 2 с намеренно** (`recovery_min_apply_delay`).
На одной машине настоящий лаг репликации — единицы миллисекунд, и устаревшее
чтение из него не увидеть. Об этом сказано прямо в выводе сцены 22, и в тексте
шага это надо повторить.

**Данные копии живут в памяти, а не в томе.** Сцена 25 промотирует её в
первичный узел, после чего вернуть копию в строй можно только пересборкой с
нуля: перезапуск контейнера чистит память, и базовая копия снимается заново.
Сцены 22 и 25 начинаются именно с этого — поэтому первые секунды прогона уходят
на пересборку.

**Кто играет роли в сценах m08.** Отправителя (`orders`), читателя (`audit`),
оператора, сторожа (`watchdog`), узлы службы (`node-a`, `node-b`) и работников
(`worker-1`, `worker-2`) играет исполнитель сцен — так же, как он всегда играл
`client`. Предмет этих сцен — поведение кластера, а не код сервиса: настройка
подтверждения (`acks`), метка времени записи и аренда блокировки принадлежат
вызывающей стороне, и держать её внутри сцены честнее, чем прятать в сервис.

**Тема `order-events` в сценах m08 пересоздаётся с одной партицией** и с тем
числом копий, которое нужно сцене. Копии раскладываются по узлам явно
(`kafka-1` — лидер, `kafka-2` — вторая копия, `kafka-3` держит кворум), иначе
кластер каждый раз выбирает узлы сам и имена в выводе разъезжаются. Партиции —
предмет m06; здесь предмет копии.

**Ключи в быстрой памяти** (профиль `cache`) — их видно в выводе сцен:

| Ключ | Что лежит |
|---|---|
| `card:<ресторан>` | карточка ресторана: сколько заказов и на какую сумму |
| `read:order:<номер>` | проекция: то, что читатель видит вместо владельца данных |
| `lock:order:<номер>` | аренда распределённой блокировки |
| `fence:order:<номер>` | счётчик выдач аренды — он же ограждающий номер |

Таблица `lab_assignments` (`order_id`, `courier`, `fence`) — хранилище сцены 29.
Она не принадлежит сервису заказов: назначение курьера в ней пишут работники
сцены, и на ней же показано, как хранилище отвергает запись с устаревшим
номером.

**Термины, которыми названы механизмы** (в тексте шагов те же самые):
синхронный набор копий (ISR), небезопасный выбор лидера, ограждающий номер
(fencing token), один поход за всех (single flight), окно рассогласования.

## Сцены 22–29 — числа для текста шагов (m08–m09)

| Сцена | Что задано | Что наблюдается |
|---|---|---|
| 22 | копия отстаёт на 2 с; чтения на 0,5, 1,5 и 3,0 с | два ответа «нет такого заказа», третий — «заказ на месте»; тот же запрос к первичному узлу отвечает сразу |
| 23 | тема в 2 копиях, синхронных требуется 2; выключен `kafka-2` | `acks=all` → `NOT_ENOUGH_REPLICAS`, `acks=1` → принято; в логе 3 записи из 4 (`7714`, `7716`, `7717`) |
| 24 | тема в 2 копиях, синхронная требуется 1; 2 записи при обеих копиях, 3 при одной | принято 5, после небезопасного выбора лидера осталось 2, потеряно 3 |
| 25 | разрыв, promote копии, запись в обе половины | заказ `7714` в двух версиях: `paid` на одном узле, `cancelled` на другом |
| 26 | часы `node-b` отстают на 3 с; события `paid`(+0,0), `assigned`(−2,4), `cancelled`(+1,2) | по времени записавшего порядок `assigned → paid → cancelled`, по номеру события — верный |
| 27 | применение события 700 мс, поток из 10 заказов | на спокойной системе окно меньше секунды, на потоке — 7 с |
| 28 | 100 читателей, запрос в базу 200 мс, кэш живёт 1,5 с | без защиты 100 походов в базу, с одним походом за всех — 1 |
| 29 | аренда 2 с, работа 5 с | держателей двое в обоих прогонах; без номера в базе `Айгуль`, с номером — `Пётр` |

**Времена в сценах 23 и 24 сценарные с широким допуском:** остановка и подъём
узла брокера занимают на разных машинах от нескольких секунд до полуминуты.
Содержание при этом наблюдаемое: число записей в логе, состав синхронного
набора и имя лидера — настоящие.

**Метки времени в сцене 26 сцена ставит сама** (от начала прогона, со сдвигом
для отставших часов) и печатает как смещение — `+1.2 с`, `−2.4 с`. Настенное
время печатать нельзя: оно разное в каждом прогоне. Часы контейнеров при этом
не переводятся: в Docker это невозможно, и в тексте шага об этом сказано прямо.

## Инстансов больше одного (профиль `scale`, m10)

С m10 служба перестаёт быть одним процессом. Это не «ещё один сервис»: образ,
настройки и база у второго инстанса те же самые — и ровно поэтому видно, что
одинаковые процессы ведут себя по-разному.

**Кого видит студент:**

| Имя | Кто это |
|---|---|
| `orders-1`, `orders-2` | два инстанса сервиса заказов; отличаются только именем в ответе |
| `balancer` | балансировщик перед ними |

**Как студент узнаёт, кто ответил.** Каждый ответ подписан заголовком
`X-Lab-Instance`. Это заголовок, а не поле тела: тело ответа у всех инстансов
обязано быть одинаковым, иначе они не взаимозаменяемы уже по контракту.

**Две точки входа балансировщика — одна на каждый уровень:**

| Порт | Уровень | Решение принимается |
|---|---|---|
| 8080 | L7 | на каждом запросе |
| 8081 | L4 | на каждом соединении |

Оба порта смотрят на одну и ту же пару инстансов. Разница между сценами
целиком в том, что видит балансировщик, — и в тексте шага это надо называть
именно так, а не «nginx умеет и так, и так».

**Кэш карточки живёт в двух местах, и место — переключатель сцены.**
`cache_mode: shared` — общая быстрая память (m09, ключ `card:<ресторан>` в
`redis`), видна всем инстансам сразу. `cache_mode: local` — память процесса,
своя у каждого инстанса. Локальный кэш инстанс сбрасывает при записи сам, но
только свой: дотянуться до памяти соседнего процесса нечем, и это не упущение
стенда, а свойство приёма.

**Балансировщик перезапускается в начале каждой сцены.** Круг он раздаёт от
внутреннего счётчика, и счётчик помнит прошлую сцену. Без перезапуска один и
тот же прогон начинался бы то с первого инстанса, то со второго — вывод стало
бы нельзя сверять с текстом шага.

**Шарды в сцене 32 играет исполнитель сцен** — как он всегда играл `client`.
У каждого шарда свой работник и своя очередь; записи настоящие и лежат в
таблице `lab_shards` (`shards`, `shard`, `restaurant`), которая после прогона
удаляется. Отдельных узлов базы под шарды в стенде нет намеренно: предмет урока
— раскладка ключей, а не эксплуатация восьми контейнеров.

## Сцены 30–32 — числа для текста шагов (m10)

| Сцена | Что задано | Что наблюдается |
|---|---|---|
| 30 | ключ живёт 3 с, карточка стоит 200 мс; запись через 2 с после первого чтения | из 6 запросов после записи 4 вернули «заказов 8», 2 — «заказов 5»; расхождение кончилось само, когда истёк ключ второго инстанса |
| 31 | 4 долгих соединения, по первому 25 запросов, по остальным по 5 | соединений поровну (2 и 2) в обоих прогонах; запросы на L7 — 20 к 20, на L4 — 30 к 10 |
| 32 | 200 заказов, 60% на «Пекарня «Батон»», запись стоит шарду 15 мс | на 4 шардах самый нагруженный принял 140 из 200, на 8 — 120; три шарда из восьми не получили ни одного ключа |

**Раскладка ключей по шардам** (остаток от деления `fnv32a` имени ресторана —
воспроизводится на любой машине):

| Ресторан | Заказов | 4 шарда | 8 шардов |
|---|---|---|---|
| `Пекарня «Батон»` | 120 | 0 | 0 |
| `Плов-хаус` | 20 | 0 | 4 |
| `Суши на углу` | 20 | 2 | 2 |
| `Кофейня «Пар»` | 20 | 1 | 1 |
| `Бургерная «Гриль»` | 20 | 1 | 5 |

Отсюда главное число сцены: удвоение шардов сняло с горячего шарда соседний
ключ (140 → 120), а сам горячий ключ остался неделимым. В тексте шага это надо
сказать именно так — «стало меньше на соседа», а не «не изменилось».

**Времена в сцене 32 сценарные, числа записей и ключей наблюдаемые.** Скорость
базы и число ядер у всех разные; допуск по времени — ±1,5 с.

## Сквозной контекст и трейсы (профиль `trace`, m11)

С m11 у запроса появляется то, чего у него не было все предыдущие модули, —
**имя, которое переживает границу процесса**. Всё остальное в профиле выросло
из этого: дерево отрезков работы, метка в строке лога, порядок разбора
инцидента.

**Кого видит студент:**

| Имя | Кто это |
|---|---|
| `jaeger` | приёмник и хранилище трейсов; интерфейс на <http://localhost:16686> |
| `orders`, `courier`, `notifier` | те же три сервиса, что и в m06–m07; новых нет |

Порт 16686 — **единственный порт стенда, который студент открывает в
браузере**. В тексте шага так и говорится: сцена печатает то же дерево, что
показывает интерфейс, и одно можно проверить другим.

**Слова, которыми курс называет вещи** (в тексте шага — только эти):

| Слово | Что это в стенде |
|---|---|
| трейс | вся работа по одному запросу, у неё один идентификатор на 16 байт |
| спан | один отрезок работы: имя, начало, длительность, родитель |
| контекст (трассировки) | пара «трейс + текущий спан», которая едет между процессами |
| `traceparent` | заголовок W3C, которым контекст едет: `00-<трейс>-<спан>-01` |

**Имена спанов** — часть контракта с текстом шага, менять их нельзя:

| Спан | Кто заводит | Когда |
|---|---|---|
| `POST /orders` | orders | входящий запрос |
| `запись заказа` | orders | вставка заказа в свою базу |
| `публикация order.paid` | orders | отправка события в лог |
| `обработка order.paid` | courier | получение события из лога |
| `назначение курьера` | courier | назначение — и событием, и синхронным вызовом |
| `POST /notifications` | notifier | входящий запрос |
| `провайдер уведомлений` | notifier | обращение к внешнему провайдеру |
| `списание в эквайринге` | orders | обращение к внешнему эквайрингу |

Два последних — **спаны за внешнего участника**: ни эквайринг, ни провайдер
уведомлений о трассировке не знают и знать не обязаны, поэтому отрезок за них
заводит вызывающий. Иначе в дереве на месте самой долгой работы была бы дыра.

**Имя спана — маршрут, а не путь.** `/orders/7714` печатается как
`GET /orders/{id}`: иначе у каждого заказа заводилась бы своя операция, и
любая сводка по ним рассыпалась бы. В тексте шага это стоит назвать прямо —
это частая ошибка на практике.

**Служебные ручки стенда своих трейсов не заводят.** `/_lab/…`, `/health` и
`/ready` без входящего контекста спанов не порождают — иначе половина
собранного была бы опросом контейнеров каждые две секунды. Но входящий
контекст они уважают: синхронный шаг саги ходит именно в такую ручку
(`/_lab/assign`), и в дереве он есть — под именем `назначение курьера`.

**Контекст в сообщении — решение отправителя, и это переключатель сцены.**
`propagate: false` — событие уходит в лог без заголовков; `propagate: true` —
с заголовком `traceparent`. По HTTP контекст едет сам собой и ни одного
решения не требует; через брокер он не едет никак, пока отправитель его туда
не положил. Ровно на этой разнице стоит сцена 33.

**Лог с меткой трассировки.** Каждый сервис держит хвост своих строк
(`GET /_lab/log?trace=<id>`), и метку строка получает не от программиста, а из
контекста запроса. Сцена 34 выбирает строки именно по метке — не по времени и
не по слову в тексте.

**Метрики в стенде считает сцена, а не Prometheus.** Числа блока 1 сцены 34
(запросов, ошибок, p50/p95/p99 по эндпойнтам) измерены клиентом — то есть самой
сценой. В жизни те же числа отдаёт служба, и в тексте шага эту оговорку надо
сделать: в клиентских числах сидит ещё и сеть до службы. Отдельного сборщика
метрик в стенде нет намеренно ([[ADR.md]]).

## Сцены 33–34 — числа для текста шагов (m11)

| Сцена | Что задано | Что наблюдается |
|---|---|---|
| 33 | один заказ на прогон; прогоны отличаются только `propagate` | 7 отрезков работы в обоих прогонах; в первом они легли в 2 несвязанных трейса, во втором — в один; разрыв ровно на брокере, а пара «курьеры → уведомления» остаётся вместе в обоих |
| 34 | 60 оформлений и 60 чтений; эквайринг отвечает 40 мс, провайдер уведомлений 12 мс, каждое 20-е обращение к нему — 2300 мс | p50 оформления 60 мс, p95 68 мс, p99 2,35 с при нуле ошибок; чтение здорово (1/2/2 мс); в дереве 98% времени в спане `провайдер уведомлений`; по метке трейса находятся 2 строки лога — в `notifier` и в `orders` |

**Заказы сцены 34:** первый — 7714, разбираемый (двадцатый) — **7733**. Медленных
запросов ровно 3 из 60 — это 5%: p95 их не поймал, p99 поймал. Это число в
тексте шага считается, а не называется.

**Времена в блоке 2 сцены 34 сценарные, структура дерева наблюдаемая.** Скорость
машины у всех разная; сцена сверяет длительность всего запроса (2,0–3,2 с) и
длительность виновного спана (2,2–2,9 с), а печатает сценарные значения.
Наблюдаемое и настоящее здесь — имена спанов, кто их завёл, вложенность,
число отрезков и обе строки лога.
