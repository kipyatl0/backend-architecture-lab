{
  "id": "37",
  "render": "table",

  "config": {
    "amount": 1890,
    "client": "Ирина",
    "stranger": "Марат",
    "restaurant": "Пекарня «Батон»",
    "first_order_id": 7714
  },

  "fields": {
    "owner_gen": "Ирины",
    "stranger_gen": "Марата"
  },

  "header": [
    "В базе три заказа: {{target}} и {{third}} — заказы {{owner_gen}}, {{second}} — заказ {{stranger_gen}}",
    "client — приложение клиента снаружи; reports — соседний сервис внутри той же сети",
    "Прогоны отличаются одной настройкой сервиса: проверяет ли он право сам"
  ],

  "tables": [
    {
      "title": "БЛОК 1 · ПРАВО ПРОВЕРЯЕТ ТОЛЬКО ПЕРИМЕТР\nСервис считает, что запрос, который до него дошёл, уже кем-то проверен.\nДве строки из пяти — те, ради которых сцена: чужой токен прошёл через шлюз,\nа сосед по сети получил те же данные, не предъявив вообще ничего",
      "columns": [
        { "title": "Кто зовёт" },
        { "title": "Как" },
        { "title": "Ответ" },
        { "title": "Что вернулось" }
      ],
      "rows": [
        { "key": "r1.own",
          "cells": ["client · токен {{owner_gen}}", "через шлюз", "{{r1.own_code}} · {{r1.own_by}}", "{{r1.own_body}}"],
          "checks": [{ "field": "code", "value": 200, "min": 200, "max": 200 }] },
        { "key": "r1.foreign",
          "cells": ["client · токен {{stranger_gen}}", "через шлюз", "{{r1.foreign_code}} · {{r1.foreign_by}}", "{{r1.foreign_body}}"],
          "checks": [{ "field": "code", "value": 200, "min": 200, "max": 200 }] },
        { "key": "r1.no_token",
          "cells": ["reports · без токена", "через шлюз", "{{r1.no_token_code}} · {{r1.no_token_by}}", "{{r1.no_token_body}}"],
          "checks": [{ "field": "code", "value": 401, "min": 401, "max": 401 }] },
        { "rule": true, "key": "r1.direct",
          "cells": ["reports · без токена", "мимо шлюза, изнутри сети", "{{r1.direct_code}} · {{r1.direct_by}}", "{{r1.direct_body}}"],
          "checks": [{ "field": "code", "value": 200, "min": 200, "max": 200 }] },
        { "key": "r1.bulk",
          "cells": ["reports · без токена", "мимо шлюза, изнутри сети", "{{r1.bulk_code}} · {{r1.bulk_by}}", "{{r1.bulk_body}}"],
          "checks": [{ "field": "code", "value": 200, "min": 200, "max": 200 }] }
      ]
    },
    {
      "title": "БЛОК 2 · СЕРВИС ПРОВЕРЯЕТ ПРАВО САМ\nТе же вызывающие при одной изменённой настройке сервиса; последняя попытка — новая.\nСмотри столбец «Ответ»: отказ выдаёт сервис, а не шлюз",
      "columns": [
        { "title": "Кто зовёт" },
        { "title": "Как" },
        { "title": "Ответ" },
        { "title": "Что вернулось" }
      ],
      "rows": [
        { "key": "r2.own",
          "cells": ["client · токен {{owner_gen}}", "через шлюз", "{{r2.own_code}} · {{r2.own_by}}", "{{r2.own_body}}"],
          "checks": [{ "field": "code", "value": 200, "min": 200, "max": 200 }] },
        { "key": "r2.foreign",
          "cells": ["client · токен {{stranger_gen}}", "через шлюз", "{{r2.foreign_code}} · {{r2.foreign_by}}", "{{r2.foreign_body}}"],
          "checks": [{ "field": "code", "value": 403, "min": 403, "max": 403 }] },
        { "rule": true, "key": "r2.direct",
          "cells": ["reports · без токена", "мимо шлюза, изнутри сети", "{{r2.direct_code}} · {{r2.direct_by}}", "{{r2.direct_body}}"],
          "checks": [{ "field": "code", "value": 401, "min": 401, "max": 401 }] },
        { "key": "r2.forged",
          "cells": ["reports · назвался «{{owner}}»", "мимо шлюза, изнутри сети", "{{r2.forged_code}} · {{r2.forged_by}}", "{{r2.forged_body}}"],
          "checks": [{ "field": "code", "value": 401, "min": 401, "max": 401 }] }
      ]
    },
    {
      "title": "БЛОК 3 · ЧТО ВИДЕЛ У СЕБЯ СЕРВИС\nСчитает сам сервис: запрос без подтверждённого контекста пользователя пришёл не через периметр",
      "columns": [
        { "title": "Счётчик сервиса" },
        { "title": "Прогон 1", "right": true },
        { "title": "Прогон 2", "right": true }
      ],
      "rows": [
        { "key": "bypassed",
          "cells": ["Запросов пришло мимо периметра", "3", "2"],
          "checks": [{ "field": "run1", "value": 3, "min": 3, "max": 3 },
                     { "field": "run2", "value": 2, "min": 2, "max": 2 }] },
        { "key": "leaked",
          "cells": ["Из них унесли данные", "3", "0"],
          "checks": [{ "field": "run1", "value": 3, "min": 3, "max": 3 },
                     { "field": "run2", "value": 0, "min": 0, "max": 0 }] },
        { "key": "denied",
          "cells": ["Отвергнуто самим сервисом", "0", "3"],
          "checks": [{ "field": "run1", "value": 0, "min": 0, "max": 0 },
                     { "field": "run2", "value": 3, "min": 3, "max": 3 }] }
      ]
    }
  ],

  "summary": "\nИТОГ: пока право проверял только периметр, сосед по сети унёс {{leaked_n}} чужих заказа, не предъявив ничего; когда право стал проверять сам сервис, отвергнуты все {{denied_n}} попытки — и чужой токен, и сосед, и подделанный контекст",

  "legend": [
    "Сеть пропускает всех, кто в ней оказался, и другого поведения от неё ждать нельзя. Запрос, отвергнутый периметром, обслужен службой без единого возражения — разница между этими двумя строками только в том, куда его послали.",
    "Чужой токен прошёл через периметр не по ошибке: токен настоящий, предъявитель настоящий. Чей это заказ — данные, которых у периметра нет и не будет, поэтому решение о праве на конкретную запись принимается там, где эта запись лежит.",
    "Периметр и сервис отвечают на разные вопросы: периметр — «кто это», сервис — «его ли это». Ни один из двух не отвечает на чужой вопрос, и убрать любой из них значит остаться без ответа на один из двух.",
    "Контекст пользователя приезжает в сервис заголовком, и последняя строка блока 2 показывает, почему ему нужна подпись: заголовок сосед выставил себе сам, а подписать его нечем — ключ есть только у периметра.",
    "Взаимный TLS, которым сервисы подтверждают личность друг другу, стенд не поднимает намеренно: возня с сертификатами заняла бы весь урок и не добавила бы к наблюдению ничего сверх того, что видно здесь."
  ]
}
